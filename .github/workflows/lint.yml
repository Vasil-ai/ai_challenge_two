name: Lint

on:
  pull_request:
    paths:
      - "task-2/**"
      - ".github/workflows/lint.yml"
  push:
    branches: [main, master]
    paths:
      - "task-2/**"
      - ".github/workflows/lint.yml"

concurrency:
  group: lint-${{ github.ref }}
  cancel-in-progress: true

jobs:
  java-lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: task-2
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          cache-dependency-path: task-2/pom.xml

      - name: Checkstyle + SpotBugs
        run: mvn -B --no-transfer-progress -q checkstyle:check spotbugs:check
