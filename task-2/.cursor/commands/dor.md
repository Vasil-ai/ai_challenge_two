# DOR — task specification

You are executing the **/dor** workflow for this workspace.

1. Read `.cursor/skills/dor/SKILL.md` with the read tool and follow it completely.
2. Produce a **comprehensive task specification** usable as a prompt for future steps.
3. Persist it to **`docs/TASK_SPEC.md`** unless the user specified a different path in their message.

Do not skip the final **implementation prompt** appendix in the document.
